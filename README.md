
  # Hackathon Pitch Slides Design (Copy)

  This is a code bundle for ProduHacks 2026 Pitch Slides.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run build`

  Run `npm run dev` to start the development server.
  